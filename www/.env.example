# Copy to .env.local and fill in. Do not commit .env.local.

SECRET_COOKIE_PASSWORD=
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
GCS_CACHE_BUCKET=
# Google Picker (Create page – "Choose from Google Drive")
# Project number: Cloud Console → Project settings → Project number (numeric)
GOOGLE_CLOUD_PROJECT_NUMBER=
# API key: APIs & Services → Credentials → Create credentials → API key (restrict to Picker API)
GOOGLE_PICKER_DEVELOPER_KEY=