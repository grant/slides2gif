/**
 * Requests a GIF.
 * Assumes the user has logged in and has permission to access the Presentation.
 */
export const createGIFRequest = async (pubSubEvent: any) => {
  console.log('creating gif');
  console.log(pubSubEvent);
};
